<div class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      {{#link-to "application" class="navbar-brand" }}
        <img src="./assets/images/logo.png" alt="Logo" class="logo">
      {{/link-to}}
    </div>
    <div class="navbar-inner">
      <ul class="nav navbar-nav ">
        {{#if normalNavbar}}
          {{#active-link}}
            {{#link-to "scoreboard" (query-params data="totalPoints" sortingOrder="desc") class="route-margin"}}
              <h4>Scorebord</h4>
            {{/link-to}}
          {{/active-link}}
          {{#active-link}}
            {{#link-to "support" class="route-margin"}}
              <h4>Uitleg</h4>
            {{/link-to}}
          {{/active-link}}
          {{#active-link}}
            {{#link-to "maintenance" (query-params active="0") class="route-margin" }}
              <h4>Onderhoud</h4>
            {{/link-to}}
          {{/active-link}}
        {{/if}}
      </ul>
    </div>
  </div>
</div>
{{outlet}}
